<h1><%= @trip ? 'Edit Your Trip' : 'Create New Trip' %></h1>
<br />
<%= form_for @trip do |f| %>
<%= f.label :name %>
<%= f.text_field :name, required: true, autofocus: true %>
<%= f.submit %>

<% end %>